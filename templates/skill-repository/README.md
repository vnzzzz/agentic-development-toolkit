# Skill repository template

Rename this repository and the `name` field in `skill/SKILL.md` to the same lowercase hyphenated Skill name. Keep the distributable bundle under `skill/` and repository-only tests, fixtures, CI, and release tooling outside it.
