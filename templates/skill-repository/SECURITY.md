# Security policy

Treat `SKILL.md`, bundled scripts, dependencies, symlinks, and binary assets as executable supply-chain inputs. Document network, subprocess, filesystem-write, credential, and destructive-operation requirements explicitly. Do not commit credentials or mutable downloaded executables.
